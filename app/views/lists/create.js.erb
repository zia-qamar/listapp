$('#modal').modal('toggle');
<% unless @list.errors.present? %>
$('.panel-group').append("<%= escape_javascript render(partial: 'lists/lists', locals: { list: @list, list_items: [] }) %>")
display_success_msg("<%= flash[:notice]%>")
<% else %>
display_error_msg("<%= flash[:notice]%>")
<% end %>